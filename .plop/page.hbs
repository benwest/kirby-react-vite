import { usePage } from "@/components/Page"

export default function {{properCase type}}Page() {
  const page = usePage("{{dashCase type}}")
  return (
    <div>
      <h1>{{properCase type}}</h1>
      <pre>{JSON.stringify(page, null, 2)}</pre>
    </div>
  )
}
